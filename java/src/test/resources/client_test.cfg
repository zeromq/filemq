#   Configure server for plain access
#
client
    heartbeat = 1           #   Interval in seconds
    inbox = ./fmqroot/recv
    resync = 1

# inbox
#     path = ./some/directory
# subscribe
#     path = /logs
    
security
    plain
        login = guest
        password = guest
